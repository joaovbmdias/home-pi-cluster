apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: "media"
  name: "configurations"
spec:
  storageClassName: "manual"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "5Gi"