apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: "network"
  name: "network-configurations"
spec:
  storageClassName: "manual"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "5Gi"